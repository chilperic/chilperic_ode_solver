import {LESSONS} from './lessons.mjs';
const $=id=>document.getElementById(id),esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const routes={reactions:'studio.html?preset=enzyme',dynamics:'ode.html',plant:'plant-growth.html',leaf:'leaf-physiology.html',branching:'branching.html',sensitivity:'sensitivity.html',fitting:'fitting.html',bayes:'advanced-methods.html',ml:'ml.html',symbolic:'symbolic.html',sciml:'sciml.html'};
$('companionSelect').innerHTML=LESSONS.map(l=>`<option value="${l.id}">${esc(l.title)}</option>`).join('');
function render(){const l=LESSONS.find(x=>x.id===$('companionSelect').value)||LESSONS[0];$('companionContent').innerHTML=`<p class="u-eyebrow">Companion · source chapter ${l.chapter}</p><h2>${esc(l.title)}</h2><p><b>${esc(l.question)}</b></p><p class="u-small">The following companion was newly authored. Laboratory names may describe related methods rather than identical preset IDs; inspect equations and settings before transferring a calculation.</p>${l.body}<h2>Practice before opening the solution</h2><p>${esc(l.practice)}</p><details><summary>Hint</summary><p>${esc(l.hint)}</p></details><details><summary>Worked reasoning</summary><div>${l.solution}</div></details><h2>Change an assumption</h2><p>${esc(l.challenge)}</p><div class="u-exportbar"><a class="u-button" href="${routes[l.lab]||'labs.html'}">Related laboratory →</a><a class="u-button" href="book.html?chapter=${l.chapter}">Chapter & source practice →</a></div>`;window.FokoTypeset?.($('companionContent'));}
$('companionSelect').onchange=render;const id=new URLSearchParams(location.search).get('lesson');if(LESSONS.some(l=>l.id===id))$('companionSelect').value=id;render();
