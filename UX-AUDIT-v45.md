# UX audit v45

Score after patch: 88/100.

## Fixed
- Public pages no longer explain internal product-separation logic to users.
- Workbench is now the complete modeling gateway. Legacy remains available.
- Agent Lab no longer exposes a crowded method strip by default.
- Agent plot modes are model-aware and not static HTML options.
- Math Beauty topic chips are clickable and land on visible sections.

## Remaining risks
- Workbench and Legacy duplicate some links by design; this should be revisited after user testing.
- Agent Lab still needs plot export buttons and animation-speed presets.
- Some docs still repeat platform positioning and should be compressed in a later documentation pass.
