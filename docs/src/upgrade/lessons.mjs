/** Original reconstruction companions. Not transcriptions of the private source PDF. */
const L=(id,title,chapter,lab,question,body,practice,hint,solution,challenge)=>({id,title,chapter,lab,question,body,practice,hint,solution,challenge});
export const LESSONS=[
L('conservation','A model is an argument you can test',1,'reactions','Which statement must remain true even when every trajectory looks plausible?',String.raw`<p>Begin with the reversible conversion \(A\rightleftharpoons B\). Its equations are \(\dot A=-k_fA+k_rB\) and \(\dot B=k_fA-k_rB\). Adding them removes both reaction rates: \(d(A+B)/dt=0\). This is an invariant, not a visually expected pattern.</p><p>Open Reaction networks, select Reversible conversion, and compute. Change the two rates by different factors. The shape and equilibration time change, but the sum must retain its initial value. An implementation that clips a negative concentration can create or destroy material while making the trajectory look nicer.</p><h2>Verification is not validation</h2><p>Preserving the sum verifies one property of the implemented equations. It does not establish that a two-pool system adequately describes a real experiment. To validate that interpretation, define observable quantities, measurement uncertainty and a comparison dataset. Separate numerical error, parameter uncertainty and model discrepancy rather than combining them into one error bar.</p><p>For the closed lipid chain, the preserved quantity is the number of chains. An elongation step adds carbon from an unrepresented substrate, so a chain-count invariant is not a carbon-mass invariant.</p>`,
'With A(0)=0.8, B(0)=0.2, kf=0.4 and kr=0.1, determine the equilibrium and an invariant before running.',
'Use kf A*=kr B* together with A*+B*=1. Do not ask a root solver to rediscover a redundant conservation equation.',
String.raw`<p>Substitution gives \(0.4A^*=0.1(1-A^*)\), hence \(A^*=0.2\), \(B^*=0.8\). The total remains 1. Both equilibrium equations are dependent: the unconstrained two-dimensional Newton system is singular. Eliminate one variable or impose the conserved total as an independent equation.</p>`,
'What observation would falsify the two-pool closed-system assumption rather than merely suggest different rates?'),
L('solver','Time resolution is not numerical accuracy',5,'dynamics','Can a smooth curve still be a poor numerical solution?',String.raw`<p>For \(\dot x=-kx\), \(x(t)=x_0e^{-kt}\) is an exact reference. Euler advances with \(x_{n+1}=(1-kh)x_n\). The true solution stays positive; a coarse step can alternate in sign, and for \(kh>2\) the numerical magnitude grows.</p><p>The output sample count decides how often results are reported. The internal maximum step limits how far the solver advances in one numerical step. RK45 additionally reduces steps when its local error estimate is too large. Increasing the sample count can force extra substeps here, but it is not a substitute for a convergence study.</p><h2>A controlled experiment</h2><p>Choose a fixed final time, compare several step sizes against the exact endpoint, and record absolute error. In the asymptotic regime, halving a step gives roughly twice the accuracy for Euler, four times for Heun, and sixteen times for RK4. Round-off, adaptive tolerances and an output grid that forces different step sizes can obscure these ratios.</p><p>Explicit RK45 is not a universal stiff solver. This reconstruction stops on unsafe states rather than silently switching mathematics. An independent SciPy script is exported with each ODE run for a second implementation check.</p>`,
'For k=1, x0=1, T=1 and one Euler step h=1, compute the numerical endpoint and compare it with exp(−1).',
'Insert the values directly into the Euler recurrence. A solution exactly equal to zero is not exact decay at a finite time.',
String.raw`<p>Euler produces \(x_1=0\), while \(e^{-1}\approx0.367879\). The absolute error is about 0.367879. Two steps give \((1-1/2)^2=0.25\), which is closer but still inaccurate. Plotting a thousand points between the same coarse values would not repair the underlying calculation.</p>`,
'Which error tolerance is scientifically defensible relative to the measurement precision of the quantity you intend to interpret?'),
L('oscillation','Why predators do not necessarily eat every prey',5,'dynamics','What mechanism allows prey to recover after predator numbers rise?',String.raw`<p>The Lotka–Volterra system is \(\dot x=\alpha x-\beta xy\), \(\dot y=\delta xy-\gamma y\). Prey increase when predator density is below \(\alpha/\beta\); predators increase when prey density exceeds \(\gamma/\delta\). Each population therefore changes the growth conditions of the other.</p><p>In the laboratory, parameters a, b, c and d correspond to α, β, δ and γ respectively. The positive equilibrium is \((x^*,y^*)=(\gamma/\delta,\alpha/\beta)\). In the ideal classical system it is surrounded by closed orbits. These cycles are not produced by random forcing or by predators planning not to overexploit prey. They arise from the signs and delays implicit in the coupled dynamics.</p><p>Use both the time plot and the phase plane. Predict which population peaks first, then inspect the calculated trajectory. The phase plot answers a different question: which states are connected along one orbit? Changing initial conditions changes that orbit.</p><h2>The model can be wrong about nature</h2><p>There is no prey carrying capacity, seasonal forcing, age structure or discrete extinction. Near-zero real populations cannot remain smoothly fractional forever. The demo is a mechanistic explanation of a possibility, not a reliable ecosystem forecast.</p>`,
'At a state with x below γ/δ and y above α/β, what are the signs of both time derivatives?',
'Factor each equation by its own positive state and compare the remaining factor with zero.',
String.raw`<p>Both derivatives are negative: \(\alpha-\beta y<0\), and \(\delta x-\gamma<0\). Prey decrease because predators are abundant; predators decrease because prey are insufficient. Predator decline eventually allows prey to recover, provided the continuous trajectory remains positive.</p>`,
'Which added mechanism could turn persistent neutral cycles into a damped approach or a sustained attracting cycle?'),
L('water','A drought experiment must conserve water',16,'plant','Where does every millimetre of water enter and leave?',String.raw`<p>Define stored soil water \(W\), precipitation \(P\), irrigation \(I\), evapotranspiration \(E\) and drainage \(D\), in compatible units. Over a step, \(W_{n+1}=W_n+(P+I-E-D)\Delta t\). When demand exceeds supply, actual evapotranspiration must be limited by the available water. A negative store followed by clipping violates the budget.</p><p>The plant workspace records cumulative inputs and outputs and reconstructs storage independently from them. Set rain and irrigation to zero. Then water may stay constant or decrease, but cannot increase. Starting with zero water in that case must prevent water-supported growth. A climate preset must not secretly replenish the store.</p><p>The simulation horizon and life-cycle length are separate. A 180-day run with a 140-day life cycle includes post-season behavior; it is not six repeated months with automatic replanting. Inspect this boundary rather than reading every flat trajectory as a numerical failure.</p><h2>Know what is simplified</h2><p>The biomass allocation, thermal response and C3/C4/CAM settings are teaching proxies. These equations do not reproduce the creator’s full heat–hydraulics–photosynthesis research model. Named climates are illustrative scenarios, not measured site data.</p>`,
'Initial storage is 80 mm; cumulative precipitation is 35 mm, irrigation 0, evapotranspiration 60 mm and drainage 5 mm. What must the current store be?',
'Use the cumulative identity, not the final instantaneous rain rate.',
String.raw`<p>\(W=80+35+0-60-5=50\) mm. If the plotted store is 70 mm, the 20 mm discrepancy is an accounting or implementation issue, unless an additional documented flux or changed initial condition exists.</p>`,
'Would a prediction that one pathway wins survive replacing the prescribed temperature and water proxies with measured physiology?'),
L('leaf','Limiting branches in C3 assimilation',16,'leaf','Why can more CO₂ stop producing the same gain?',String.raw`<p>The reconstructed C3 leaf calculation uses the net minimum of a Rubisco-limited branch and an electron-transport-limited branch. In this fixed-temperature reduction, \(A=\min(A_c,A_j)-R_d\), where \(A_c=V_{c\max}(C_i-\Gamma^*)/[C_i+K_c(1+O/K_o)]\) and \(A_j=J(C_i-\Gamma^*)/[4(C_i+2\Gamma^*)]\).</p><p>The active branch determines the local response. Increasing Vcmax cannot increase A when electron transport remains limiting. Conversely, increasing light has little immediate effect when Rubisco capacity is limiting. This is a structural statement about a minimum, not evidence that the unused pathway is biologically unimportant.</p><p>Open Leaf physiology and vary incident light and Vcmax separately. Identify where the two net branches cross. Keep units in partial-pressure form as shown in the parameter labels. The implementation does not add a stomatal model, temperature response, TPU limit or full C4 mechanism.</p>`,
'At Ci=Γ*, what does this reduced net-assimilation model predict?',
'Both numerator differences are zero. Respiration is subtracted after selecting the branch.',
String.raw`<p>Both gross branches vanish, so \(A=-R_d\). This is not the net CO₂ compensation point when respiration is nonzero. Confusing \(\Gamma^*\) with a measured net compensation point changes the interpretation of the parameters.</p>`,
'Can a curve fit identify Vcmax when every measured point lies on the light-limited branch?'),
L('randomness','An average lineage is not a typical lineage',14,'branching','How can the mean rise while many populations disappear?',String.raw`<p>Suppose each individual leaves either zero or two offspring, with probabilities \(1-p\) and \(p\). The mean number of offspring is \(m=2p\), so \(E[Z_g]=Z_0m^g\). This statement averages over repeated realizations; it does not promise that any single lineage follows the mean.</p><p>For one ancestor, eventual extinction satisfies \(q=1-p+pq^2\). The probability is the smallest solution in [0,1]: 1 when \(p\le1/2\), and \((1-p)/p\) when \(p>1/2\). For independent initial ancestors, raise this value to \(Z_0\).</p><p>Run the supercritical example, compare the median and mean, and inspect the endpoint histogram. Repeat with more replicates rather than selecting a seed that looks persuasive. A finite-horizon extinct fraction estimates extinction by that horizon, not the eventual probability.</p><p>The displayed 5–95% band is an interval across simulated states. It is neither a confidence interval for the mean nor a posterior credible interval for p. All three would answer different questions.</p>`,
'For p=0.6 and one initial ancestor, calculate the offspring mean and eventual extinction probability.',
'The relevant quadratic root is less than one because p exceeds one half.',
String.raw`<p>The mean is \(1.2\) offspring per individual, but eventual extinction is \(0.4/0.6=2/3\). Thus the expectation grows while two-thirds of infinitely followed lineages eventually die out. A finite simulation cannot certify survival forever.</p>`,
'What would a selection experiment conclude incorrectly if it discarded all extinct replicates?'),
L('sensitivity','Sensitivity depends on the question and distribution',12,'sensitivity','Sensitive to which parameter, which output, at which time?',String.raw`<p>A local derivative such as \(\partial x(T)/\partial\theta\) is defined at one parameter vector. An elasticity rescales it by \(\theta/x(T)\), but becomes unstable when the denominator is close to zero. Neither quantity is a global importance ranking.</p><p>Variance-based indices use a specified distribution over parameters. The reconstructed Sobol demonstration assumes independent uniform ranges. First-order indices describe the variance explained by one parameter alone; total-order indices also include interactions. Small Monte Carlo samples can produce estimates outside [0,1]. Clipping them hides uncertainty and is not a correction.</p><p>For logistic growth, a long-horizon output approaches the carrying capacity and loses sensitivity to the growth rate. The same parameter can matter greatly for an early transient. Change the horizon before claiming that a rate is unimportant.</p><h2>A proper comparison</h2><p>Record the output definition, ranges, independent-sampling assumption, seed and sample budget. Repeat at larger budgets and with defensible alternative ranges. The global calculation in this reconstruction focuses on the first state at the horizon; it does not automatically summarize every trajectory feature.</p>`,
'For independent zero-mean inputs with Var(X1)=Var(X2)=1 and Y=X1+2X2, derive first-order Sobol indices.',
'No interaction is present. The two variance contributions are 1 and 4.',
String.raw`<p>\(\mathrm{Var}(Y)=5\). Hence \(S_1=1/5\), \(S_2=4/5\), and total-order indices equal first-order indices. Changing the input variances changes these values even though the functional equation remains identical.</p>`,
'Do your selected uncertainty ranges encode measurement evidence or merely convenient slider limits?'),
L('inference','A good fit can conceal non-identifiability',11,'fitting','Does reproducing observations determine the mechanism?',String.raw`<p>Least squares minimizes \(\sum_i[y_i-f(t_i;\theta)]^2\). Without weights, it treats equal residual magnitudes equally across observations. This is appropriate only when the implied observation assumptions are defensible; it is not a universal criterion for all noise.</p><p>If observations depend only on \(ab\), then every pair with the same product generates the same curve. More optimization iterations cannot make a and b separately identifiable. A precise fitted curve can coexist with very uncertain parameters.</p><p>Use linear and exponential examples first, where analytical structure is clear. Inspect residual patterns and alter initial guesses in a nonlinear example. Import a numeric CSV only after checking that the first two columns really are the explanatory variable and response. A synthetic dataset is a numerical test, not external scientific validation.</p><p>The fitting workspace reports estimates, residuals and convergence information. It does not manufacture parameter confidence intervals from an optimizer’s stopping flag. A serious analysis adds an observation model, uncertainty calculation and an appropriate validation design.</p>`,
'For exact observations y=2+3x at x=0,1,2, what residual sum of squares should a correct linear fit achieve?',
'Two independent columns [1,x] can represent these three observations exactly.',
String.raw`<p>The intercept is 2 and slope is 3; the residual sum of squares is zero up to floating-point error. Failure on this known-answer case invalidates a claim that the same implementation has reliably estimated a complex biological response.</p>`,
'Which new measurement would separate two parameter combinations that fit your current data equally well?'),
L('bayes','Update uncertainty without hiding assumptions',13,'bayes','What changes when the prior changes but observations do not?',String.raw`<p>Let k of n independent Bernoulli trials be successes. Multiplying a Beta(α,β) prior by the likelihood produces a Beta(α+k,β+n−k) posterior. Its mean is \((\alpha+k)/(\alpha+\beta+n)\). This is an exact conjugate update for a specified likelihood, not a general-purpose Bayesian model of biological dependence.</p><p>Use the same data with two priors. Compare how much the posterior changes when n is small and when n is large. A narrow posterior is not automatically a well-calibrated posterior if repeated outcomes are correlated or the sampling process is biased.</p><p>The mean and standard deviation shown here are analytical. The displayed 95% interval uses numerical integration of a plotted density grid. Near endpoint singularities, that grid is not accurate enough for rigorous tail inference; the interface warns about this case.</p>`,
'With a Beta(1,1) prior and 7 successes in 10 independent trials, determine the posterior and its mean.',
'Add successes to α and failures to β; the mean is not simply k/n.',
String.raw`<p>The posterior is Beta(8,4), and its mean is \(8/12=2/3\). The observed frequency is 0.7. The difference reflects the prior update, not a numerical error.</p>`,
'What aspect of your observation process would make independent Bernoulli trials an implausible likelihood?'),
L('leakage','Preprocessing is part of the fitted model',19,'ml','Can you leak test information without using test labels?',String.raw`<p>A polynomial ridge model constructs features, estimates their centers and scales, and fits penalized coefficients. These are all learned operations. Estimating preprocessing parameters from the entire dataset exposes the training procedure to the distribution of held-out observations.</p><p>In this workspace, the split is drawn first. Feature means and scales are computed from training rows only and then reused on test rows. The exported experiment records indices, means, scales and coefficients. This makes the split auditable rather than just displaying a test score.</p><p>Increase polynomial degree with and without regularization. Training error may decline while test error rises. Do not repeatedly inspect one held-out set and call it untouched: tuning to that set converts it into development data. Time-series, patient-level and experimental-batch datasets usually need structured splitting instead of random rows.</p><p>This demonstration is a transparent baseline, not an automatic deep-learning service. A mechanistic model that solves the scientific question may be preferable to a more flexible predictor.</p>`,
'If repeated measurements from each plant appear in both random training and test sets, what does the test estimate fail to establish?',
'Ask whether the deployment task concerns another measurement of a known plant or a completely new plant.',
'<p>It does not establish generalization to independent plants. Correlated subject-specific information can appear on both sides of the split. Group by plant before splitting when the intended task is prediction for new plants.</p>',
'Is your metric attached to the scientific deployment task, or merely the easiest partition to create?'),
L('derivatives','Compare a derivative with an independent calculation',4,'symbolic','When does a smaller finite-difference step make the result worse?',String.raw`<p>A central finite difference is \([f(x+h)-f(x-h)]/(2h)\). For a sufficiently smooth function, its truncation error scales as \(h^2\), but subtraction and round-off become important when h is very small. A log–log error curve often decreases and then rises.</p><p>The Symbolic workspace differentiates a restricted expression tree. It does not run arbitrary JavaScript, import code, or provide a full computer algebra system. Smooth elementary operations are supported; nonsmooth functions such as abs are rejected for derivative operations rather than assigned a misleading derivative at a cusp.</p><p>Symbolic differentiation, automatic differentiation and finite differences are different techniques. This workspace demonstrates the first and third. It must not be described as an automatic-differentiation implementation merely because it obtains exact algebraic derivative rules.</p>`,
'For f(x)=sin(x)+x², derive first and second derivatives, and evaluate both at x=0.',
'Use the sum rule. The sine contribution is not zero in the first derivative at the origin.',
String.raw`<p>\(f'(x)=\cos x+2x\) and \(f''(x)=-\sin x+2\). At zero, the results are 1 and 2. Compare both with the expression-tree output before trusting a longer model expression.</p>`,
'How will your optimizer behave if a piecewise model is differentiated on the wrong side of a discontinuity?'),
L('discovery','An equation discovered from data needs another test',22,'sciml','Does the recovered formula survive a new initial condition?',String.raw`<p>For scalar data x(t), the reconstruction forms a polynomial library \(\Theta=[1,x,x^2,x^3]\), estimates time derivatives, fits coefficients and repeatedly removes terms below a threshold. This is a deliberately small sparse-regression demonstration, not an unrestricted discovery engine.</p><p>On noise-free logistic data, the expected vector field is \(\dot x=0.7x-0.7x^2\). The training trajectory alone does not cover every state. The workspace therefore integrates the discovered field from a different initial condition and compares it with the synthetic reference when one exists.</p><p>Numerical differentiation amplifies noise, and nearly dependent library columns can make coefficients unstable. Vary sampling density, noise, threshold and observed state range. A small trajectory error does not establish that every recovered coefficient is meaningful.</p><p>When you upload your own trajectory, the synthetic-reference curve is removed. The application cannot know the true generating field of your measurements. It still tests whether the fitted field yields a finite trajectory from the stated new initial condition.</p>`,
'Why can a nearly constant time series be poor evidence for discovering both x and x² terms?',
'Consider the columns of the library when every observed x is close to one value.',
'<p>The columns become nearly collinear over the observed range, so different coefficient combinations can generate almost the same derivative. More decimal places cannot replace observations that explore distinct states or a scientifically justified model restriction.</p>',
'What perturbation experiment would distinguish your recovered equation from a competing model that fits the same observed trajectory?')
];
export const PROJECTS=[
 {id:'numerical',title:'Numerical trust in a mechanistic model',labs:['dynamics','reactions','steady','symbolic'],chapters:[1,4,5,24],aim:'Turn a model into a verifiable computational argument before adding biological complexity.',steps:['Declare states, units, observation quantities and at least one known-answer case.','Compare step refinement, RK45 and the exported independent SciPy solver. Include an intentionally failing coarse-step case.','Derive conservation or equilibrium structure. Explain why a singular steady-state system can follow from a conserved total.','Deliver model JSON, tests, residual plots and a limitations statement.'],boundary:'Begin with the supplied teaching equations. Incorporating original research equations requires their source and publication permission. This guide does not establish original lipid-model bistability.'},
 {id:'plants',title:'Finite resources and photosynthetic assumptions',labs:['plant','leaf','evolution','sensitivity'],chapters:[12,16,26],aim:'Separate water accounting, physiological limiting branches and assumptions about fitness.',steps:['Construct rain-free, fully watered and seasonal cases; verify water budgets before comparing biomass.','Distinguish simulation horizon from plant life-cycle length. Use temperature and water sweeps with explicit parameter ranges.','Test which C3 biochemical branch is limiting in the separate leaf workspace. Do not treat the plant proxy as the coupled leaf model.','Change the constructed evolutionary fitness assumptions. Report whether the qualitative inference is robust, including stationary or unsuccessful trajectories.'],boundary:'This is an open investigation, not a reproduction of the full research photosynthesis model. Climate scenarios are illustrative; obtain measured forcing before interpreting place-specific predictions.'},
 {id:'inference',title:'From observations to defensible inference',labs:['fitting','bayes','ml','sciml'],chapters:[11,13,19,22],aim:'Compare transparent statistical and mechanistic baselines without leaking validation information.',steps:['Specify the measurement process and reserve data using a split appropriate to subjects, batches or time.','Recover known synthetic parameters, then introduce controlled noise and diagnose residuals.','Compare a mechanistic fit with a regularized polynomial baseline using the same held-out objective.','Stress-test discovered equations with a new initial condition, uncertain derivatives and alternative libraries; retain negative results.'],boundary:'Synthetic benchmark success is not clinical, regulatory or external experimental validation. The application does not infer causal structure automatically.'},
 {id:'stochastic',title:'Stochastic evidence and a reproducible release',labs:['stochastic','branching','genetics','agents'],chapters:[9,14,15,25,27],aim:'Quantify variation across realizations and deliver a reproducible experiment another person can run.',steps:['Predict an expectation, invariant or absorption property analytically before simulation.','Record seeds and distinguish replicate intervals from uncertainty in estimated parameters. Increase the ensemble rather than choosing an attractive seed.','Compare well-mixed and spatial mechanisms without pretending that different models are different solvers of one equation.','Deliver a versioned configuration, outputs, reference checks and an explicit record of failed, blocked and unexecuted checks.'],boundary:'These are newly authored investigation guides. No finished research notebook or complete original CFSE/Cyton analysis is implied.'}
];
